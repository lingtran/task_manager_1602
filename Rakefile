require 'rake/testtask'

 Rake::TestTask.new do |t|
  t.libs << "test"
  t.pattern = "test/**/*_test.rb"
 end

 # purpose of file: defines tasks that can be easily executed more than once
 # TestTask is built into Rake, Rake is already in the computer 
